Nodejs app Serving Manifest.json as GET endpoint.

## Installation

Use the package manager [typescript](https://www.typescriptlang.org) to build project.

```bash
npm install
npx tsc -b
```

## Usage

Use node to run services locally.

```bash
node dist/index.js
